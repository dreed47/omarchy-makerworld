{
  "name": "omarchy-makerworld",
  "version": "0.1.0",
  "description": "Desktop notifications for MakerWorld (Bambu Lab) account activity. Omarchy shell plugin.",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check Model.js && node --test && python3 -m py_compile bin/_mwlib.py bin/makerworld-login bin/makerworld-refresh bin/makerworld-token && node scripts/validate-manifest.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dreed47/omarchy-makerworld.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
